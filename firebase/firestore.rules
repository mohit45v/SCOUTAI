rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for hackathon
    match /players/{playerId} {
      allow read, write: if true;
    }
    match /reports/{reportId} {
      allow read, write: if true;
    }
  }
}
